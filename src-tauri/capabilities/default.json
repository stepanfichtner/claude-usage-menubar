{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the popover window",
  "windows": ["popover"],
  "permissions": [
    "core:default"
  ]
}
